@using Microsoft.JSInterop
@inject IJSRuntime js

@code {
    [Parameter] public string Title { get; set; }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await js.InvokeVoidAsync(
                "exJsInterop.setDocumentTitle",
                Title);
        }
    }
}